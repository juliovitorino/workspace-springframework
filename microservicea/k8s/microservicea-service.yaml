apiVersion: v1
kind: Service
metadata:
  name: microservicea-service
spec:
  type: NodePort
  selector:
    app: microservicea # <- same name in deployment
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080

